# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project State

This is a nascent project directory. The only artifact so far is a custom Claude Code skill scaffold at `.claude/skills/facebook-manager/SKILL.md` (currently empty).

## Custom Skills

Skills live under `.claude/skills/<skill-name>/SKILL.md`. Each `SKILL.md` defines what the skill does and how it should be invoked. When the skill is fleshed out, document its trigger conditions, inputs, and expected behavior in that file.
